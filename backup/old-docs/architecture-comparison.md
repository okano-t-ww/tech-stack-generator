# フロントエンドアーキテクチャパターン比較

## 比較対象パターン

1. **Feature-Sliced Design (FSD)** - 機能スライス設計
2. **Atomic Design** - 原子設計
3. **Domain-Driven Design (DDD)** - ドメイン駆動設計
4. **Clean Architecture** - クリーンアーキテクチャ
5. **Layered Architecture** - 階層化アーキテクチャ
6. **Module-Based (現状)** - モジュールベース

---

## 1. Feature-Sliced Design (FSD)

### 構造

```
src/
├── app/              # アプリケーション初期化
├── pages/            # ページコンポーネント（ルーティング）
├── widgets/          # 独立した機能ブロック
├── features/         # ユーザーシナリオ・機能
├── entities/         # ビジネスエンティティ
├── shared/           # 再利用可能なコード
└── processes/        # 複数ページにまたがるプロセス
```

### 本プロジェクトへの適用

```
src/
├── app/
│   ├── layout.tsx
│   └── page.tsx
│
├── widgets/
│   └── tech-stack-generator/     # ジェネレーター全体
│       ├── ui/
│       │   └── TechStackGenerator.tsx
│       └── model/
│           └── useGenerator.ts
│
├── features/
│   ├── tech-selection/            # Tech選択機能
│   │   ├── ui/
│   │   │   └── TechSelector.tsx
│   │   └── model/
│   │       └── useTechSelection.ts
│   │
│   ├── markdown-generation/       # Markdown生成機能
│   │   ├── ui/
│   │   │   └── MarkdownOutput.tsx
│   │   └── model/
│   │       └── useMarkdownGenerator.ts
│   │
│   └── icon-display/              # アイコン表示機能
│       ├── ui/
│       │   └── TechIconGrid.tsx
│       └── model/
│           └── useTechIcon.ts
│
├── entities/
│   └── tech/                      # Techエンティティ
│       ├── model/
│       │   ├── types.ts
│       │   └── techStack.ts
│       ├── api/
│       │   └── iconifyApi.ts
│       └── lib/
│           └── validators.ts
│
└── shared/
    ├── ui/                        # 共有UIコンポーネント
    ├── lib/                       # ユーティリティ
    └── config/                    # 設定
```

### メリット
- ✅ **標準化された構造**: チーム開発で理解しやすい
- ✅ **スケーラビリティ**: 機能追加が明確
- ✅ **依存関係が明確**: 下位層は上位層に依存しない
- ✅ **ドキュメントが豊富**: 公式ガイドが充実

### デメリット
- ❌ **学習コスト**: 独自の概念（widgets, entities等）
- ❌ **小規模には過剰**: 本プロジェクトには大げさかも
- ❌ **ディレクトリ深い**: ネストが深くなる傾向

### 適用難易度
⭐⭐⭐ (中) - 概念の理解が必要だが、ドキュメント豊富

---

## 2. Atomic Design

### 構造

```
src/
├── components/
│   ├── atoms/           # 最小単位（Button, Input）
│   ├── molecules/       # 原子の組み合わせ（SearchBox）
│   ├── organisms/       # 複雑なコンポーネント（Header）
│   ├── templates/       # ページレイアウト
│   └── pages/           # 実際のページ
├── hooks/
├── utils/
└── types/
```

### 本プロジェクトへの適用

```
src/
├── components/
│   ├── atoms/
│   │   ├── TechIcon.tsx
│   │   └── TechToggle.tsx
│   │
│   ├── molecules/
│   │   ├── TechIconGrid.tsx
│   │   └── CategoryFilter.tsx
│   │
│   ├── organisms/
│   │   ├── TechSelector.tsx
│   │   └── MarkdownGenerator.tsx
│   │
│   ├── templates/
│   │   └── GeneratorTemplate.tsx
│   │
│   └── pages/
│       └── GeneratorPage.tsx
│
├── hooks/
│   ├── useTechSelection.ts
│   └── useMarkdownGeneration.ts
│
├── services/
│   ├── techStackService.ts
│   └── iconifyService.ts
│
├── constants/
│   └── techStack.ts
│
└── types/
    └── tech.ts
```

### メリット
- ✅ **デザインシステムとの親和性**: UIライブラリ構築に最適
- ✅ **再利用性が高い**: コンポーネントの粒度が明確
- ✅ **直感的**: デザイナーとの協業がしやすい

### デメリット
- ❌ **ビジネスロジックの配置が不明確**: hooks/servicesが別
- ❌ **分類が曖昧**: molecule vs organism の境界
- ❌ **コンポーネント以外の配置**: データ層の扱いが弱い

### 適用難易度
⭐ (易) - 概念がシンプル

---

## 3. Domain-Driven Design (DDD)

### 構造

```
src/
├── domain/              # ドメイン層
│   ├── tech/
│   │   ├── entities/
│   │   │   └── Tech.ts
│   │   ├── value-objects/
│   │   │   ├── TechId.ts
│   │   │   └── IconifyId.ts
│   │   ├── repositories/
│   │   │   └── TechRepository.ts
│   │   └── services/
│   │       └── TechDomainService.ts
│   │
│   └── markdown/
│       └── services/
│           └── MarkdownService.ts
│
├── application/         # アプリケーション層
│   ├── use-cases/
│   │   ├── SelectTech.ts
│   │   └── GenerateMarkdown.ts
│   └── dto/
│       └── TechDto.ts
│
├── infrastructure/      # インフラ層
│   ├── repositories/
│   │   └── InMemoryTechRepository.ts
│   └── api/
│       └── IconifyApi.ts
│
└── presentation/        # プレゼンテーション層
    ├── components/
    ├── hooks/
    └── pages/
```

### 本プロジェクトへの適用

```
src/
├── domain/
│   └── tech-stack/
│       ├── entities/
│       │   ├── Tech.ts              # エンティティ
│       │   └── TechCollection.ts    # 集約
│       ├── value-objects/
│       │   ├── TechId.ts
│       │   ├── IconifyId.ts
│       │   └── TechLink.ts
│       ├── repositories/
│       │   └── ITechRepository.ts   # インターフェース
│       └── services/
│           └── TechSelectionService.ts
│
├── application/
│   └── use-cases/
│       ├── SelectTechUseCase.ts
│       ├── GenerateMarkdownUseCase.ts
│       └── FilterByCategoryUseCase.ts
│
├── infrastructure/
│   └── repositories/
│       └── StaticTechRepository.ts  # 実装
│
└── presentation/
    └── components/
        └── ...
```

### メリット
- ✅ **ビジネスロジックが明確**: ドメイン知識が集約
- ✅ **テスタビリティ**: 依存性注入で単体テスト容易
- ✅ **拡張性**: 複雑なビジネスルールに対応

### デメリット
- ❌ **過剰設計**: 小規模プロジェクトには重い
- ❌ **学習コスト高**: エンティティ、値オブジェクト等の概念
- ❌ **ボイラープレート多**: コード量が増える

### 適用難易度
⭐⭐⭐⭐⭐ (難) - DDD の深い理解が必要

---

## 4. Clean Architecture

### 構造

```
src/
├── core/                # エンティティ・ビジネスルール
│   ├── entities/
│   └── use-cases/
│
├── adapters/            # インターフェース層
│   ├── controllers/
│   ├── presenters/
│   └── gateways/
│
├── frameworks/          # 外部フレームワーク
│   ├── ui/
│   └── database/
│
└── main.ts              # 依存性注入
```

### 本プロジェクトへの適用

```
src/
├── core/
│   ├── entities/
│   │   └── Tech.ts
│   └── use-cases/
│       ├── SelectTech.ts
│       └── GenerateMarkdown.ts
│
├── adapters/
│   ├── controllers/
│   │   └── TechSelectionController.ts
│   ├── presenters/
│   │   └── MarkdownPresenter.ts
│   └── repositories/
│       └── TechRepositoryImpl.ts
│
├── frameworks/
│   ├── ui/
│   │   └── components/
│   └── data/
│       └── techStackData.ts
│
└── di/
    └── container.ts
```

### メリット
- ✅ **依存関係の逆転**: 外部への依存を排除
- ✅ **テスタビリティ最高**: 完全な分離
- ✅ **フレームワーク非依存**: 将来的な移行が容易

### デメリット
- ❌ **複雑すぎる**: 本プロジェクトには過剰
- ❌ **ファイル数が多い**: 小さな変更でも多くのファイル
- ❌ **実装コスト**: 依存性注入の設定が大変

### 適用難易度
⭐⭐⭐⭐⭐ (難) - アーキテクチャの深い理解が必要

---

## 5. Layered Architecture (階層化)

### 構造

```
src/
├── presentation/        # プレゼンテーション層
│   ├── components/
│   └── pages/
│
├── application/         # アプリケーション層
│   ├── services/
│   └── use-cases/
│
├── domain/              # ドメイン層
│   ├── models/
│   └── services/
│
└── infrastructure/      # インフラ層
    ├── api/
    └── storage/
```

### 本プロジェクトへの適用

```
src/
├── presentation/
│   ├── components/
│   │   ├── TechSelector.tsx
│   │   ├── TechIconGrid.tsx
│   │   └── MarkdownOutput.tsx
│   └── hooks/
│       ├── useTechSelection.ts
│       └── useMarkdownGenerator.ts
│
├── application/
│   └── services/
│       ├── TechStackService.ts
│       ├── MarkdownService.ts
│       └── IconifyService.ts
│
├── domain/
│   ├── models/
│   │   ├── types.ts
│   │   └── TechStack.ts
│   └── validators/
│       └── techValidators.ts
│
└── infrastructure/
    ├── data/
    │   └── techStackData.ts
    └── api/
        └── iconifyClient.ts
```

### メリット
- ✅ **シンプルで理解しやすい**: 階層が直感的
- ✅ **適度な分離**: 過剰設計にならない
- ✅ **実装が容易**: すぐに始められる

### デメリット
- ❌ **水平分割の限界**: 機能が増えると各層が肥大化
- ❌ **横断的関心事**: 複数層にまたがる機能が扱いにくい

### 適用難易度
⭐⭐ (易) - 概念がシンプル

---

## 6. Module-Based (現状改良版)

### 構造

```
src/
├── modules/             # 機能モジュール
│   └── tech-stack/
│       ├── components/
│       ├── hooks/
│       ├── services/
│       ├── types/
│       └── index.ts     # Public API
│
├── shared/              # 共有コード
│   ├── components/
│   ├── hooks/
│   └── utils/
│
└── core/                # コア機能
    ├── config/
    └── providers/
```

### 本プロジェクトへの適用

```
src/
├── modules/
│   └── tech-stack-generator/
│       ├── components/
│       │   ├── TechSelector.tsx
│       │   ├── TechIconGrid.tsx
│       │   └── MarkdownOutput.tsx
│       ├── hooks/
│       │   ├── useTechSelection.ts
│       │   └── useMarkdownGenerator.ts
│       ├── services/
│       │   ├── techStackService.ts
│       │   └── iconifyService.ts
│       ├── constants/
│       │   └── techStackData.ts
│       ├── types/
│       │   └── index.ts
│       └── index.ts
│
├── shared/
│   ├── ui/
│   │   └── (shadcn components)
│   ├── components/
│   │   ├── Combobox.tsx
│   │   └── DndList.tsx
│   └── utils/
│       └── cn.ts
│
└── core/
    ├── providers/
    │   └── ThemeProvider.tsx
    └── config/
        └── constants.ts
```

### メリット
- ✅ **最もシンプル**: 学習コストゼロ
- ✅ **柔軟性が高い**: プロジェクトに合わせて調整可能
- ✅ **移行が容易**: 現状から段階的に改善

### デメリット
- ❌ **標準がない**: チームごとに解釈が異なる
- ❌ **スケーラビリティ**: 大規模化で構造が曖昧に

### 適用難易度
⭐ (易) - 現状の延長線上

---

## 総合比較表

| 観点 | FSD | Atomic | DDD | Clean | Layered | Module |
|------|-----|--------|-----|-------|---------|--------|
| **学習コスト** | 中 | 低 | 高 | 高 | 低 | 最低 |
| **実装コスト** | 中 | 低 | 高 | 高 | 低 | 最低 |
| **スケーラビリティ** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| **保守性** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **テスタビリティ** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **チーム開発** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |
| **ドキュメント** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| **小規模適性** | ⭐⭐ | ⭐⭐⭐⭐ | ⭐ | ⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **大規模適性** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |

---

## プロジェクト特性による推奨

### 本プロジェクトの特性
- **規模**: 小〜中規模
- **複雑性**: 中程度（Tech Stack管理のみ）
- **チーム**: 個人または小規模
- **成長性**: 機能追加の可能性あり

### 推奨順位

#### 🥇 1位: **Layered Architecture** (階層化)

**理由:**
- ✅ シンプルで理解しやすい
- ✅ Service層の導入で十分な分離
- ✅ 過剰設計にならない
- ✅ 現状からの移行が容易

**具体的な構造:**
```
src/
├── presentation/
│   ├── components/
│   └── hooks/
├── application/
│   └── services/
├── domain/
│   ├── types/
│   └── constants/
└── infrastructure/
    └── api/
```

#### 🥈 2位: **Module-Based (改良版)**

**理由:**
- ✅ 最も移行が容易
- ✅ 柔軟性が高い
- ✅ 学習コストゼロ
- ⚠️ ただし、明確なルールが必要

**具体的な構造:**
```
src/
├── modules/tech-stack-generator/
│   ├── components/
│   ├── hooks/
│   ├── services/
│   └── index.ts
├── shared/
└── core/
```

#### 🥉 3位: **Feature-Sliced Design (簡略版)**

**理由:**
- ✅ 将来の拡張性が高い
- ✅ 標準化された構造
- ⚠️ 学習コストは必要
- ⚠️ 小規模には少し重い

**具体的な構造:**
```
src/
├── features/
│   ├── tech-selection/
│   └── markdown-generation/
├── entities/tech/
└── shared/
```

---

## 推奨アプローチ

### 段階的導入戦略

#### Phase 1: Service層の導入（Layered Architecture ベース）

**即座に実装可能で効果が高い:**

```typescript
// application/services/TechStackService.ts
export class TechStackService {
  static getById(id: TechId): TechItem | undefined { ... }
  static filterByCategories(categories: TechCategory[]): TechItem[] { ... }
  static search(keyword: string): TechItem[] { ... }
}

// application/services/IconifyService.ts
export class IconifyService {
  static getIconUrl(iconifyId: IconifyId, size?: number): string { ... }
}
```

**メリット:**
- コンポーネントからビジネスロジックを分離
- テストが書きやすくなる
- 再利用性が向上

#### Phase 2: Hooks の整理

```typescript
// presentation/hooks/useTechSelection.ts
export function useTechSelection() {
  const [selected, setSelected] = useState<TechItem[]>([]);
  const toggle = (tech: TechItem) => { ... };
  return { selected, toggle, ... };
}
```

#### Phase 3: 必要に応じて FSD 的な構造に移行

将来的に規模が大きくなったら、Feature-Sliced Design を検討。

---

## 結論

### 本プロジェクトに最適な選択

**🎯 推奨: Layered Architecture + Service層**

```
src/
├── presentation/           # UI層
│   ├── components/
│   │   ├── generator/
│   │   ├── layout/
│   │   └── theme/
│   └── hooks/
│       ├── useTechSelection.ts
│       └── useMarkdownGenerator.ts
│
├── application/            # アプリケーション層
│   └── services/
│       ├── TechStackService.ts
│       ├── IconifyService.ts
│       └── MarkdownService.ts
│
├── domain/                 # ドメイン層
│   ├── types/
│   │   └── tech.ts
│   ├── constants/
│   │   └── techStack.ts
│   └── validators/
│       └── iconValidators.ts
│
└── shared/                 # 共有コード
    ├── ui/
    └── utils/
```

### 移行の優先順位

1. **Phase 1**: Service層を作成（2-3時間）
2. **Phase 2**: Hooksを整理（1-2時間）
3. **Phase 3**: ディレクトリ再編成（2-3時間）

合計: **5-8時間** で完了可能

---

## 次のアクション

どのアプローチで進めますか？

1. **Layered Architecture で進める**（推奨）
2. **Module-Based で軽量に進める**
3. **FSD を試してみる**
4. **他の組み合わせを検討する**

または、具体的な実装例を見たいパターンはありますか？
